# Public API domain used by Caddy and backend CORS allowlist.
# Example: api.example.com
SERVICE_DOMAIN=localhost

# Optional: when the Next app calls the Go API on another origin (local dev with split ports).
# NEXT_PUBLIC_BACKEND_API_URL=http://127.0.0.1:8080
